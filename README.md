**To specify the profile, run commands with 'env' property, e.g.:**  
*gradlew build -Penv=stable*  
*gradlew clean -Penv=dev*

P.s. "dev" profile is used by default, when the 'env' property isn't specified.